# deps
# Node.js dependencies
node_modules
.pnpm-store
.pnpm-debug.log

# Keep pnpm-lock.yaml
!pnpm-lock.yaml

# build
dist/
build/
out/
storybook-static/

# ignore README for publishing
codex-cli/README.md

# ignore Nix derivation results
result

# editor
.vscode/
.idea/
.history/
.zed/
*.swp
*~

# cli tools
CLAUDE.md
.claude/
AGENTS.override.md

# caches
.cache/
.turbo/
.parcel-cache/
.eslintcache
.nyc_output/
.jest/
*.tsbuildinfo

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*
!.env.example

# package
*.tgz

# ci
.vercel/
.netlify/

# patches
apply_patch/

# coverage
coverage/

# personal files
personal/

# os
.DS_Store
Thumbs.db
Icon?
.Spotlight-V100/

# Unwanted package managers
.yarn/
yarn.lock

# release
package.json-e
session.ts-e
CHANGELOG.ignore.md

# nix related
.direnv
.envrc

# Gradle and Kotlin build artifacts (all subprojects)
**/.gradle/
**/.kotlin/
**/build/
.android-sdk/
local.properties

# Build logs
build_log*.txt
/*.hprof

# ast_distance build artifacts
tools/ast_distance/build/
tools/ast_distance/ast_distance
tools/ast_distance/ast_distance.real

# ast_distance temporary output files
*.scan_output.txt
.scan_output.txt
all_matched.txt
files_with_headers.txt
tasks.json
tools/ast_distance/deep_*.txt
tools/ast_distance/deep_current.txt
tools/ast_distance/missing_current.txt

# Generated port reports
port_status_report.md
high_priority_ports.md
NEXT_ACTIONS.md
deep_report.txt

# ktreesitter plugin build output
ktreesitter-kotlin/ktreesitter-plugin/bin/
/tmp/

# Swift Package Manager / Xcode workspace state.
#
# `swift-test-harness/` is a real SPM package that exercises the Kotlin
# Swift Export pipeline against the artifacts produced by
# `embedSwiftExportForXcode`. Its `Package.swift`, `Tests/`, and
# per-package `.gitignore` (which ignores its own `.build/`) are all
# tracked. The patterns below cover the Swift/Xcode workspace state
# that this harness — or any future Swift Package added under the
# repo — would also produce.
#
# Note: the staging directory the Kotlin Swift Export pipeline writes
# to (`build/swift-test/lib*.a` + `.swiftmodule/` bundles when run with
# the Xcode-style env vars) is already covered by `build/` above.
.swiftpm/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
