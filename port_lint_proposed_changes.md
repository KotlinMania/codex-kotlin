# port-lint Proposed Changes

**Generated:** 2026-04-27
**Source:** codex-rs/protocol/src/conversation_id.rs
**Target:** src/nativeMain/kotlin/ai/solace/coder/protocol/ConversationId.kt

These are review proposals only. They are emitted when a Rust -> Kotlin pair matches only after fallback normalization, so the existing `port-lint` header is not an exact provenance match.

| Target file | Current header | Proposed header | Source path | Reason |
|-------------|----------------|-----------------|-------------|--------|
| `src/nativeMain/kotlin/ai/solace/coder/protocol/ConversationId.kt` | `// port-lint: source conversationId.rs` | `// port-lint: source conversation_id.rs` | `conversation_id.rs` | `port-lint provenance header matched only after fallback normalization: 'conversationId.rs' vs expected 'conversation_id.rs'` |
